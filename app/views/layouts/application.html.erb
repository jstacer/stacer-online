<!DOCTYPE html>
<html class="no-js">
  <head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="description" content="">
		<meta name="viewport" content="width=device-width, initial-scale=1">

		<title><%= full_title(yield(:title)) %></title>

		<%= render 'layouts/rails_default' %>
		<%= render 'layouts/shim' %>
		<%= render 'layouts/favicon' %>
		<%= render 'layouts/scripts' %>
  </head>

  <body>
		<%= render 'layouts/header' %>
		
		<div class="container">
		  <!-- MAIN view template-->
		  <%= yield %>
		  <%#= debug(params) if Rails.env.development? %>
		</div>

		<!-- Only animate footer on home page -->
		<% if controller_name == "static_pages" && action_name == "home" %>
			<footer class="wow fadeInUp" data-wow-delay=".8s">
		<% else %>
			<footer>
		<% end %>
			<div class="container text-center">
			  <div class="row">
					<div class="col-md-12">
						<p>Copyright © 2017 Jud Stacer  &nbsp;&nbsp;|&nbsp;&nbsp;  
							 <a href="https://draftbernie.org/" target="_blank" id="Bernie">
							 	Keep On Bernin'
							 </a>
						</p>
					</div>
			  </div>
			</div>
		</footer>

		<script>
			// Bernie pop-up text
		  $('#Bernie').hover(      
		    function () {
		      $('#bernie-text').css({"display":"block"});
		    }, 
		    function () {
		      $('#bernie-text').css({"display":"none"});
		    }
		  );
	  </script>
  </body>
</html>
